'use strict';

const num1 = 5;
const num2 = 15;

function addValues() {
  console.log(`Sum is :${num1 + num2}`);
}
addValues();

function getPersonalInfo(name, birthYear) {
  console.log(`Hi ${name} you born in ${birthYear}`);
}
getPersonalInfo('Abhishek', 1998);
